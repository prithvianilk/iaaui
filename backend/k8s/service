apiVersion: v1
kind: Service
metadata:
  name: {image_name}-loadbalancer
spec:
  type: LoadBalancer
  selector:
    app: {image_name}-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 80